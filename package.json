{
  "name": "folio",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate && node -e \"require('fs').writeFileSync('.output/public/.nojekyll','')\"",
    "deploy": "yarn generate && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/bootstrap": "^0.0.1",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "core-js": "^3.49.0",
    "nuxt": "^4.4.2",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "@babel/eslint-parser": "^7.28.6",
    "@nuxtjs/eslint-config": "^12.0.0",
    "eslint": "^10.2.0",
    "sass": "^1.99.0",
    "sass-loader": "^16.0.7",
    "vue-svg-loader": "^0.16.0",
    "vue-template-compiler": "^2.7.16"
  }
}
